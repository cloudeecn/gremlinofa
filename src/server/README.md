# `src/server/` — Node WebSocket backend (Phase 2 stub)

Empty placeholder. Phase 2 lands a Node entry point, WebSocket transport,
SQLite storage adapter, and filesystem VFS adapter here. The directory
exists already so the four-layer lint rules can reference it.

See `plans/jiggly-crunching-brooks.md` for the full Phase 2 scope.
