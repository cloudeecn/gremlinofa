# Server port (default: 3001)
PORT=3001

# CORS origin (empty = same-domain only, * = allow all, or specific origin like https://example.com)
CORS_ORIGIN=

# SQLite database file path (default: ./data.db)
DB_PATH=./data.db
